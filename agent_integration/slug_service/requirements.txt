# Serving deps for the deployed demo. Reranker (CrossEncoder) IS kept, so quality
# matches the evaluated baseline. torch is installed CPU-only in the Dockerfile
# (avoids the ~2GB CUDA build).
fastapi==0.115.14
uvicorn==0.35.0
langchain-openai==0.3.17
langchain-community==0.3.24
langgraph==0.4.5
faiss-cpu==1.7.4
rank-bm25==0.2.2
sentence-transformers==5.2.2
ddgs==9.14.4
openai==1.75.0
