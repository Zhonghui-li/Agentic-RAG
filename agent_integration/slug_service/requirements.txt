# Serving deps for the deployed demo. Reranker (CrossEncoder) IS kept, so quality
# matches the evaluated baseline. torch is installed CPU-only in the Dockerfile
# (avoids the ~2GB CUDA build).
# numpy<2 — faiss-cpu 1.7.4 is built against NumPy 1.x; a fresh container pulls
# NumPy 2.x, which breaks faiss import (AttributeError: _ARRAY_API not found).
numpy==1.26.4
fastapi==0.115.14
uvicorn==0.35.0
langchain-openai==0.3.17
langchain-community==0.3.24
langgraph==0.4.5
# pin the langgraph sub-packages too — an unpinned newer langgraph-prebuilt expects
# langgraph._internal (missing in 0.4.5) -> ModuleNotFoundError at startup
langgraph-prebuilt==0.1.8
langgraph-checkpoint==2.0.26
langgraph-sdk==0.1.69
faiss-cpu==1.7.4
rank-bm25==0.2.2
sentence-transformers==5.2.2
ddgs==9.14.4
openai==1.75.0
# LLM observability — traces (latency, token cost, tool usage) auto-activate in the
# container because observability.py is key-gated on the LANGFUSE_* env vars.
langfuse==4.7.1
# pgvector store — used when DATABASE_URL is set (catalog lives in Postgres, not the
# image). faiss-cpu stays installed for the import + local fallback.
psycopg[binary]==3.3.4
pgvector==0.4.2
